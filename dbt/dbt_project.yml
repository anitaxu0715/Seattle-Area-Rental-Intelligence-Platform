name: seattle_rental_intelligence
version: "1.0.0"
config-version: 2

profile: seattle_rental_intelligence

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  seattle_rental_intelligence:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: int
      +materialized: table
    marts:
      +schema: marts
      +materialized: table
