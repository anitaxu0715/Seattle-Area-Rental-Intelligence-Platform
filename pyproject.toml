[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "seattle-rental-intelligence"
version = "1.0.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["src*"]
